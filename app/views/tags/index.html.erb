<h1>All Tags</h1>

<ul id="articles">
    <% @tags.each do |tag| %>
        <li>
            <%= link_to  tag.name, tag_path(tag), class: 'article_title', id: "tag_#{tag.id}" %>
        </li>
    <% end %>
</ul>
<br />
<%= link_to  "Home Page", articles_path %>
